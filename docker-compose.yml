version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=false
    # For development, you can add:
    # volumes:
    #   - .:/app
    # command: python app.py